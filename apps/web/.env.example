# ============================================================================
# ProtocolFit — variáveis de ambiente do frontend (opcionais em desenvolvimento)
# ============================================================================

# URL base da API do backend Fastify (padrão abaixo já funciona no dev)
NEXT_PUBLIC_URL_API=http://localhost:3333/api
