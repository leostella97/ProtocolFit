/**
 * postcss.config.mjs
 * ---------------------------------------------------------------------------
 * Configuração do PostCSS para o Tailwind CSS v4.
 * O Tailwind v4 usa o plugin @tailwindcss/postcss (não há mais tailwind.config).
 * ---------------------------------------------------------------------------
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
